# 데이터 출처와 한계

## 1. 지금 화면에 보이는 것

`data/jobs.json` 의 항목이 `sample: true` 이면 **실제 채용공고가 아니다.** 화면 구성과 분류·점수 로직을 확인하기 위한 가상의 항목이며, 카드에 `샘플` 배지가 붙고 상단에 배너가 뜬다. 수집기를 한 번 성공적으로 실행하면 샘플은 전부 사라지고 수집 결과로 대체된다.

샘플 카드의 링크는 **해당 기업의 실제 채용 홈 주소**로 연결된다. 존재하지 않는 개별 공고 URL을 만들어 넣지 않았다.

## 2. 출처 우선순위

1. 기업 공식 채용 홈페이지
2. 공공 취업정보 API 원문
3. 이용약관상 허용된 채용 플랫폼 공식 API
4. 기업정보·공시·공식 보고서 (기업 규모, 인재상, 연봉)

같은 공고가 여러 곳에 있으면 기업명·공고명·근무지·마감일을 비교해 하나로 합치고, 그룹 안에 기업 공식 페이지가 있으면 그것을 대표 링크로 쓴다.

## 3. 수집 원칙

- 요청 전에 해당 호스트의 `robots.txt` 를 읽고, 허용되지 않은 경로는 **요청 자체를 하지 않는다**. `robots.txt` 를 읽지 못하면 보수적으로 전면 차단한다.
- 동일 호스트에 최소 1.5초 간격을 둔다.
- 공고 본문을 통째로 저장하지 않는다. 업무/자격/우대는 항목당 160자, 최대 6항목으로 요약해 보관하고 항상 원문 링크를 함께 둔다.
- 공식 API가 있는 플랫폼은 API만 쓴다. 약관상 크롤링이 허용되지 않는 사이트는 수집하지 않으며, 그 사실을 `meta.json` 의 소스 상태에 `수집 미허용`으로 기록한다.

## 4. 만들어내지 않는 것

| 항목 | 확인되지 않을 때의 표기 |
|---|---|
| 기업 규모 | `분류 미확인` |
| 규모 분류 근거 | 근거를 자동 검증하지 못하면 규모 옆에 `*` 표시 + 상세에 `자동 검증되지 않음` |
| 인재상 | `확인 가능한 공개 정보 없음` |
| 초봉·연봉 | `확인 가능한 공개 정보 없음` (추정치를 넣지 않는다) |
| 마감일 | `마감일 미확인` |
| 경력·고용형태 | `구분 미확인` / `고용형태 미확인` |
| 원문 링크 | `원문 링크 미확인` |

`data/companies.json` 에서 `size_source.verified: false` 는 "근거가 알려져 있으나 이 저장소가 자동으로 확인하지는 않았다"는 뜻이다. LG에너지솔루션·삼성SDI·SK온의 대기업 분류는 공정거래위원회 공시대상기업집단 소속이라는 널리 알려진 사실에 근거하지만, 원문 공시를 자동 대조하지는 않았으므로 `verified: false` 로 둔다. 나머지 기업은 전부 `분류 미확인` 이다.

## 5. 검증되지 않은 부분 (사용 전 반드시 확인)

- `collector/src/config.js` 의 API 엔드포인트와 파라미터명은 **문서로 검증되지 않았다.** 그래서 모든 소스가 `verified: false` 로 시작하고 기본적으로 건너뛴다.
- `mapApiItem()` 의 응답 필드 후보는 흔한 형태를 추정한 것이다. 실제 응답을 보고 맞춰야 한다.
- `CAREER_SITES` 의 `selectors` 는 비어 있다. 대부분의 대기업 채용 페이지는 JavaScript로 목록을 그리므로 정적 HTML 파싱이 되지 않는다.

## 6. 자동 갱신이 실패하면

기존 `data/jobs.json` 을 **지우지 않는다.** `meta.json` 의 `run_status` 만 `failed` 로 바뀌고 마지막 성공 시각과 실패 사유가 화면 상단 배너에 표시된다. 수집 결과가 0건이어도 마찬가지로 기존 데이터를 유지한다.

## 7. 적합도 점수의 성격

점수는 공고 텍스트와 프로필 키워드의 매칭에 기반한 **참고용 우선순위 지표**이지 합격 가능성 예측이 아니다. 공고 본문이 짧거나 요약이 추출되지 않으면 실제보다 낮게 나올 수 있다. 지원 판단은 항상 원문을 열어서 하는 것을 전제로 한다.

## 8. 개인정보

프로필·관심공고·지원상태는 브라우저 `localStorage` 에만 저장된다. 서버로 전송되지 않고, 저장소에도 커밋되지 않으며, 기기 간 동기화되지 않는다. 브라우저 데이터를 지우면 함께 사라진다.
